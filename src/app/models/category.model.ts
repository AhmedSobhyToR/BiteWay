export interface Category{
    categoryId: string;
    categoryName: string;
    imagePath: string;
}

export interface CategoryBackend{
    CategoryId: string;
    CategoryName: string;
    ImagePath: string;
}